<h5>注文履歴一覧</h5>

<% if @orders.count == 0 %>
  <div class='mx-auto max-w-3xl'>
    <p class='mb-8 text-xl text-center'>ご注文はありません</p>
    <div class='flex justify-center'>
      <%= link_to '商品一覧へ', public_items_path %>
    </div>
  </div>
<% else %>
  <table class='table'>
  <thead>
    <tr>
      <th>注文日</th>
      <th colspan="4">配送先</th>
      <th>注文商品</th>
      <th>支払金額</th>
      <th>ステータス</th>
      <th>注文詳細</th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.created_at.strftime('%Y/%m/%d') %></td>
        <td>
          〒<%= order.postal_code %></br>
          <%= order.address %></br>
          <%= order.name %>
        </td>
        <td>
          <% @cart_items.each do |cart_item| %>
            <%= cart_item.items.name %>
          <% end %>
        </td>
        <td><%= order.total_payment %>円</td>
        <td><%= order.status %></td>
        <td>
          <%= link_to '表示する', order_path(order.id) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<% end %>
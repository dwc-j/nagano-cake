<% obj.each do |item| %>
  <table>
    <tr>
      <td>
        <%= image_tag item.image, size: "250x200" %>
      </td>
      <td>
        <%= link_to admin_item_path(item) do%>
          <%= item.name %>
        <% end %>
      </td>
      <td>￥<%= number_to_currency(item.price, format: "%n") %></td>
    </tr>
  </table>
<% end %>